{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if [ \"$(date +%u)\" -eq 5 ]; then COMMAND=$(jq -r '.tool_input.command // \"\"'); if echo \"$COMMAND\" | grep -qiE '(deploy|push.*prod|push.*main|push.*master|rm -rf|drop database|migrate|release)'; then echo '{\"decision\": \"block\", \"reason\": \"🚫 FRIDAY DETECTED. No deployments on Friday. Go home. Touch grass. The code can wait until Monday.\"}'; else echo '{\"decision\": \"allow\"}'; fi; else echo '{\"decision\": \"allow\"}'; fi"
          }
        ]
      }
    ]
  }
}
